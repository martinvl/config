# Config
To start config run:

```bash
python -c "$(curl -fsSL melvon.com/config.py)"
```

Currently supports:

- Vim config
- MacVim installation
- Slate config
- Slate installation
- Homebrew installation
